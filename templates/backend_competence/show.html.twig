{% extends 'base.html.twig' %}

{% block title %}Competence{% endblock %}

{% block body %}
    <h1>Competence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ competence.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ competence.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_competence_index') }}">back to list</a>

    <a href="{{ path('app_backend_competence_edit', {'id': competence.id}) }}">edit</a>

    {{ include('backend_competence/_delete_form.html.twig') }}
{% endblock %}
