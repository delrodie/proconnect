<form method="post" action="{{ path('app_backend_competence_delete', {'id': competence.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette compétence?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competence.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
