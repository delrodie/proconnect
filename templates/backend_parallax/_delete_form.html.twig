<form method="post" action="{{ path('app_backend_parallax_delete', {'id': parallax.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce parallax?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parallax.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
