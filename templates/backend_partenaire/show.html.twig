{% extends 'base.html.twig' %}

{% block title %}Partenaire{% endblock %}

{% block body %}
    <h1>Partenaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partenaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ partenaire.nom }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ partenaire.media }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_partenaire_index') }}">back to list</a>

    <a href="{{ path('app_backend_partenaire_edit', {'id': partenaire.id}) }}">edit</a>

    {{ include('backend_partenaire/_delete_form.html.twig') }}
{% endblock %}
