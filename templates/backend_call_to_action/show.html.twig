{% extends 'base.html.twig' %}

{% block title %}CallToAction{% endblock %}

{% block body %}
    <h1>CallToAction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ call_to_action.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ call_to_action.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ call_to_action.content }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ call_to_action.media }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ call_to_action.type }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ call_to_action.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_call_to_action_index') }}">back to list</a>

    <a href="{{ path('app_backend_call_to_action_edit', {'id': call_to_action.id}) }}">edit</a>

    {{ include('backend_call_to_action/_delete_form.html.twig') }}
{% endblock %}
