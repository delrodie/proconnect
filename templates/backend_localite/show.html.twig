{% extends 'base.html.twig' %}

{% block title %}Localite{% endblock %}

{% block body %}
    <h1>Localite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ localite.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ localite.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ localite.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_localite_index') }}">back to list</a>

    <a href="{{ path('app_backend_localite_edit', {'id': localite.id}) }}">edit</a>

    {{ include('backend_localite/_delete_form.html.twig') }}
{% endblock %}
